import { Routes } from '@angular/router';
import { WindowComponent } from './pages/terminal/window/window.component';

export const routes: Routes = [
    {
        path: '',
        component: WindowComponent,
    },
];
